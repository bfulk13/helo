-- DROP TABLE users
-- DROP TABLE posts

-- CREATE TABLE users (
--     id serial primary key,
--     username varchar(20),
--     password varchar(20),
--     profile_pic text
-- );

-- CREATE TABLE posts (
--     id serial primary key,
--     title varchar(45),
--     img text,
--     content text,
--     author_id integer,
--     FOREIGN KEY (author_id) REFERENCES users(id) 
-- );

-- INSERT INTO users (username, password, profile_pic)
-- VALUES (fulkules, test, https://images.unsplash.com/photo-1503286666306-61c9985f16cc?ixlib=rb-1.2.1&auto=format&fit=crop&w=1000&q=80)

